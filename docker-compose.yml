services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: stationery_db
      POSTGRES_USER: stationery
      POSTGRES_PASSWORD: Qwe123.,
    ports:
      - "14100:5432"
    volumes:
      - stationery_postgres_data:/var/lib/postgresql/data

volumes:
  stationery_postgres_data: